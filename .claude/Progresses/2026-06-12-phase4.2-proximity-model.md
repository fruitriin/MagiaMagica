# 進捗表

## 運用ルール

### タスク開始時
1. `.claude/Feedback.md` を読み、前回の改善アクションで未対応のものがあれば考慮する
2. 以下の手順で Markdown チェックリストを作成する
   1. 1ショットで作業できる範囲にサブタスクを分割する
   2. 並行作業できる粒度でさらに分割する
   3. 各サブタスクにテスト作成・統合テスト・Lint・ビルドが必要か検討し、必要なら追加する
   4. 必要に応じて 2.1〜2.3 を再帰的に適用する

### 作業中
3. サブタスク着手時に `- [x]` でチェックしていく。並列可能なタスクはコンテナオーケストレーションを利用する
4. 実装フェーズの最終サブタスク完了時、以下の知見を `/addf-knowhow` で記録する（既存 knowhow の更新も含む）:
   - **コーディング知見**: 実装中に発見した再利用可能なパターン、落とし穴、技術的判断とその根拠

### エージェント起動時の共通ルール
- エージェントチーム（TeamCreate）やサブエージェント（Agent）を作成するとき、各エージェントへのプロンプトに **最初に `/addf-knowhow-index` を実行する** よう指示を含めること
- これにより各エージェントがプロジェクトの知見ベースを把握した状態で作業を開始できる

### タスク完了時 — 品質検証

4. プロジェクトのビルド・Lint・テストコマンドを実行する
   - **失敗した場合 → 実装に差し戻す**。原因分析 → 修正 → 再実行
5. `addf-code-review-agent` でコードレビューを実施する
6. `addf-contribution-agent` で ADD フレームワークへのコントリビューション候補を検出する
7. レビュー指摘への対応:
   - **Critical/High**: 必ずこのフェーズ内で修正する（先送り禁止）
   - **Medium**: 原則修正。先送りする場合は独立計画を起こす
   - **Low/Info**: Plan に記録し、必要に応じて独立計画で対応
   - **バグ分離**: 発見されたバグが現在のプランと関心事が異なる場合は、修正せずに新しいプラン（`docs/plans/`）を書き起こし、`TODO.md` に追加するのみで現在のプランを完了させる
   - 修正後、ビルド・Lint・テストを再実行して通過を確認する
8. 品質ゲートで得た知見を `/addf-knowhow` で記録する:
   - **品質ゲート知見**: レビューエージェントが検出したパターン（セキュリティ、コード品質、分離パターン違反等）のうち、他のタスクでも再発しうるもの

#### ノウハウ蓄積

9. 投入されたタスクのPlanに実装完了状況を反映する
10. タスク全体の総括知見を `/addf-knowhow` で記録する:
    - **タスク総括**: 計画と実装のギャップ、想定外だった点、次回同種タスクへの教訓。コーディング・品質ゲートで既に記録した知見と重複しないこと

#### フィードバック記録

11. `.claude/Feedback.md` にPlan, TODO, Progress推進エンジンの問題の記録・改善アクションを追記する。反映済みの項目は削除する
12. `.claude/Feedback.md` にプロジェクト進行上の問題の記録・改善アクションを追記する。反映済みの項目は削除する
13. Progress 推進エンジン自体に関するフィードバック・ノウハウがあれば、テンプレート（`.claude/templates/ProgressTemplate.md`）の改善案を `.claude/Feedback.md` に記録する

#### アーカイブとコミット

14. `.claude/Progresses/YYYY-MM-DD-プラン名.md` にリネームして移動し、`.claude/templates/ProgressTemplate.md` から新規の Progress.md を作成する
15. コミットする

---

## タスク

### Phase 4.2 — 近接度モデル (docs/plans/phase4.2-proximity-model.md)

**設計確定 (着手時、計画書からの差分)**: ① Proximity トレイトは作らない — 4.1 実装結果の確定方針どおり `classify_neighbors` の中身を連続値モデルに差し替える (計画書は 4.1 前の trait 前提で古い)。② 計画書の「Phase 3.4 DataflowIr の call_edges」は実在しない (dataflow は関数内 def/use のみ) — 呼び出し関係は magia-rust の index walker + summon の call 収集から**新規 API `call_graph`** で取る。③ 距離の意味論 (0.5/0.7/1.0/1.4、min 合成) は計画書どおり proximity.rs に集約、リング離散化 (≤0.6/≤1.1/他 → 1/2/3) は描画側 (ir_export) に置く。④ serve は GoodSnapshot に call_edges をキャッシュ (reload 時1回構築)。

**実装フェーズ (Rust)**
- [x] 1. magia-rust::index: `call_graph(source)` — walker 流用で各関数本体から call site を収集し、同ファイル関数へ解決 (`.method` → name / `Self::x` → impl_context 置換 / qualified or name 照合、複数一致は定義順先頭 = resolveCall 互換)。テスト (直接呼び出し / メソッド / Self:: / 未解決は出ない)
- [x] 2. magia-core::proximity: 連続値モデルへ差し替え — Neighbor.distance: f32、WEIGHTS 定数 (同impl 0.5 / 呼び出し 0.7 / 同ファイル 1.0 / 2ホップ 1.4)、無向 BFS 2ホップ、min 合成。テスト6本 (同impl / 別impl / 直接 / 2ホップ / 循環 / 同名別impl)
- [x] 3. ir_export::focus_layout: 連続距離 → リング離散化 (境界 0.6/1.1)、3リング対応 (margin 90/190/290、scale 0.55/0.45/0.35、opacity 0.85/0.72/0.6)。NeighborChip JSON の distance はリング番号 1/2/3 (Vue 契約維持)
- [x] 4. serve: GoodSnapshot に call_edges キャッシュ + render_spell へ配線。統合テスト (呼び出し関係が距離に効く)
- [x] 5. Stage 1 ゲート全通過 (cargo 17 / clippy / fmt / ADDF / vp check / vitest 33 / build / playwright 18)。テスト8本 (proximity) + call_graph 2本 + serve 統合1本追加。viewBox は実際に使われた最遠リング基準に修正 (4.1 レビュー S1 対応) (cargo / clippy / fmt / ADDF / vp check / vitest / build / playwright)

**確認 + 素材**
- [x] 6. e2e 影響なし (リング番号非依存のセレクタ、compute→helper は中リングへ移動して通過) (fixture: compute→helper が呼び出し距離になる)。必要なら更新
- [x] 7. 素材撮影 (write_document 呼び出し7関数=中リング / Shared::spell_json 3層) → 送付。判定待ち (midchilda.rs — write_document の周辺が「呼び出し先 = 中リング」になる図) → 送付

**品質検証 + 完了処理**
- [x] 8. Stage 2 レビュー: Critical なし。対応 — W1 syn 2回パースを function_index_with_calls に統合 (1パース、call_graph 公開は廃止) / W2 同名メソッドは呼び出し元 impl 優先の3段照合 (回帰テスト追加) / S1 BFS 2段展開の意図をコメント明示。S2 (clamp) / S3 (clone 1回) は実害なしで見送り。CallEdge 型エイリアス新設 (type_complexity)。contribution 検出: 候補なし。ゲート再実行全通過
- [x] 9. 計画 memo (境界 0.9 変更・天井効果・4.4/4.5 引き継ぎ)、knowhow (call graph 近似解決の3段フォールバック)、Feedback / TODO 更新、アーカイブ、コミット
