# Phase 3.0 — 仕様書 v0.3 への昇格

## 出典

- `project-docs/magia/phase2plus-notes-v0.1.md` 付録 A (Phase 3 昇格指定: §4 CI 統合 → spec §9、§7 描画様式 → spec §14)
- `.claude/Feedback.md` の spec v0.3 宿題 (Phase 2.1: 監視エラー時挙動の明文化)
- Phase 2 実装で確定した事実 (FilterSpec の重複エラー等は v0.2 追記済み、差分確認)

## 目的

Phase 3 実装 (3.1〜3.5) の契約となる仕様増補を v0.2 と同じ「基底 + 増補差分」方式で起こす。

## スコープ

### やること

- notes §4 → **spec §9 CI 統合の正式化**: PR コメント投稿のフロー (ベース/PR 両解析 →
  変更関数のみ生成 → 差分画像投稿 → メトリクス表 → しきい値 fail)、
  しきい値の最小主義 (「ツールは判決を下さず注意を誘導する」との整合、fail 対象の限定列挙)
- notes §7 → **spec §14 描画様式の正式化**: 二大可視化軸 (Call Graph / Data Flow Graph)、
  ミッドチルダ式 = ノード中心 / ベルカ式 = エッジ・力場中心の対応、§7.5 の自動推奨
  ヒューリスティクス (Phase 3 では推奨メッセージのみ、自動切替はしない)、§7.6 力場の
  レンダリング (放射状グラデーションのアディティブ合成)
- **差分エンジンの契約** (3.1 の前提): 差分の語彙 (追加/削除/変更 Sigil・Edge、
  メトリクス delta)、決定論性、`SigilId` はリビジョン間で安定しない前提の対応付け戦略
  (スナップショット差分は ID でなく構造マッチングで取る方針を明記)
- **DSL `highlight:` の文法確定** (3.2 の前提): `highlight: changed` (差分由来) の最小形
- v0.2 宿題の解消: dev-server の監視エラー時挙動 (「変化したかもしれない」側に倒す) を §7 に追記
- 差分一覧・バージョン履歴・INDEX 更新

### やらないこと

- Phase 4 以降のセクション (多言語アダプタ・動的解析) の昇格
- 実装の変更

## 受け入れ基準

- [x] `project-docs/magia/spec-v0.3.md` が存在し §9 / §14 / 差分契約 / highlight 文法を含む
- [x] Feedback の spec v0.3 宿題 (監視エラー挙動) が解消されている
- [x] 整合レビュー (Critical 0 / High 2 / Medium 4 / Low 3 / Info 3) を通過し全件反映

## 後続

- Phase 3.1〜3.5 は本仕様を契約として参照する

## 実装結果メモ (2026-06-11)

- v0.2 と同じ増補差分方式 (基底 v0.1 → 第1増補 v0.2 → 本書)。フォールバック規則を冒頭に明記
- **§5.4 追補を新設** (計画外、レビュー指摘): 位置共有制約は「同一式の内側でのみ」適用と
  明確化 (ベルカ式導入で v0.2 §5.4 の暗黙の前提が崩れるため)
- notes §7 と spec §7 の節番号衝突による誤読をヘッダで注意書き (レビュー H-1)
- `highlight: changed` は notes の `changed_in_pr` を名称統合 (別語彙にしない判断を明記)
- しきい値を unsafe 新規追加のみに絞った理由 (複雑度=定数未確定、ホットパス=Phase 5 依存) を
  仕様本文に明記 (レビュー M-4)
- 節順序は v0.2 レビューの教訓 (昇順を守る) を最初から適用…のつもりが §5.4 を後置して
  同じ指摘を自分で再現しかけた。最終版で昇順に修正済み
